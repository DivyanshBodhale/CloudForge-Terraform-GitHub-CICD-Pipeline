name: code-chekout-linting 

on:
  push:
    branches: [ main ]

jobs:
  code-checkout:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: npm install

      - name: Run Linter
        run: npm run lint

    